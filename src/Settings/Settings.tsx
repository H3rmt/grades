import React, {useState} from 'react';

function Analysis() {
	const [open, setOpen] = useState(false);

	return (<div>
		Settings<br/>
		Settings<br/>
		Settings<br/>
	</div>);
}

export default Analysis;