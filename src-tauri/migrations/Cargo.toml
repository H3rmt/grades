[package]
name = "migration"
version = "0.0.1"
edition = "2021"
publish = false

[lib]
name = "migrations"
path = "src/lib.rs"

[dependencies]
tokio = { version = "1.21.2", features = ["full"] }
sea-orm-migration = { version = "0.10.1", features = ["sqlx-sqlite", "runtime-tokio-native-tls"] }