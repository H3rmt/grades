[package]
name = "migration"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "migrations"
path = "src/lib.rs"

[dependencies]
tokio = { version = "1.21.0", features = ["full"] }
sea-orm-migration = { version = "0.9.2", features = ["sqlx-sqlite", "runtime-tokio-native-tls"] }