{
	"name": "workflows",
	"version": "1.0.0",
	"description": "Workflow files for GitHub workflows",
	"repository": "https://github.com/H3rmt/grades/tree/main/workflows",
	"author": "H3rmt",
	"license": "MIT",
	"private": true,
	"scripts": {
		"dist": "esbuild src/check-version-release-check-latest-release-version.ts src/publish-create-release.ts src/publish-publish-release.ts --bundle --outdir=src --platform=node",
		"check": "tsc"
	},
	"dependencies": {
		"@actions/core": "^1.10.0",
		"@actions/github": "^5.1.1"
	},
	"devDependencies": {
		"typescript": "^4.9.4",
		"esbuild": "^0.16.15"
	}
}
